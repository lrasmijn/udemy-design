#ifndef CLASSTYPEBUILDER
#define CLASSTYPEBUILDER

#include <string>
#include <ostream>
public:
  CodeBuilder(const string& class_name)
using namespace std;

class CodeBuilder
{
  {
      // todo
  }

  CodeBuilder& add_field(const string& name, const string& type)
  {
    // todo
  }

  friend ostream& operator<<(ostream& os, const CodeBuilder& obj)
  {
    // todo
  }
};

#endif // CLASSTYPEBUILDER
